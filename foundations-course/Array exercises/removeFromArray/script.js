function removeFromArray (arr) {
  for (let i = 0; i < arr.length; i++) {
    let newArr = 0;
    
    newArr = arr.split([2]);
    newArr = arr.join 
    //.join?? but where lmao

    return newArr;
  }
}